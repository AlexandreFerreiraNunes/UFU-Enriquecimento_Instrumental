/*int D2 = 2;

void setup() {
pinMode (D2, OUTPUT) ;

// faixa padrão de valores:
// & bits(valores de O a 255)
// analoguriteRange (1024):

// 10 bits( valores de O a 1023)
}

void loop() {
analogWrite(D2, 0);
delay(2000);

analogWrite(D2,50);
delay (2000);

analogWrite(D2,100);
delay(2000);

analogWrite(D2,150) ;
delay (2000);

analogWrite(D2,200) ;
delay (2000);
}
*/