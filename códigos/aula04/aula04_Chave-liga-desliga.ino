/*int BOTAO = 4;
int LED = 2;
bool estadoLed = 0;

void setup()
{
  pinMode(BOTAO, INPUT);
  pinMode(LED, OUTPUT);
}

void loop()
{
  if(digitalRead(BOTAO) == HIGH)
  {
    estadoLed = !estadoLed;
    digitalWrite (LED, estadoLed);
    while(digitalRead(BOTAO) == HIGH);
    delay(100);
  }
}
*/